# План: Доработка импорта товаров

Ветка: feature/import-enhancement

## Задачи

- [ ] **Задача 1: Улучшенная команда products:import**
  - Ключевое поле — SKU (не slug)
  - Автогенерация slug из name (Str::slug) если не указан
  - Валидация: name и sku обязательны, category_id exists
  - Поддержка маппинга колонок из командной строки: `--map=name:col_a,sku:col_b`
  - Логирование каждого обработанного товара

- [ ] **Задача 2: MoonShine-страница импорта с маппингом**
  - Загрузка CSV → парсинг заголовков → показ UI маппинга
  - Dropdown для каждой колонки: сопоставить с полем товара
  - Поля: name*, sku*, category_id, base_price, description, is_active, meta_title, meta_description, пропустить
  - После подтверждения — запуск импорта, отчёт о результате

- [ ] **Задача 3: Кнопки экспорта/импорта на ProductIndexPage**
  - Кнопка «Экспорт CSV» → скачивание
  - Кнопка «Импорт CSV» → переход на страницу импорта
